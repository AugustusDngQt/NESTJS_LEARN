export enum UserGender {
  Male = 'Male',
  Female = 'Female',
  Other = 'Other',
}

export enum UserVerifyStatus {
  Unverified = 'Unverified',
  Verified = 'Verified',
  Banned = 'Banned',
}

export enum UserRole {
  USER = 'USER',
  ADMIN = 'ADMIN',
  HR = 'HR',
}
